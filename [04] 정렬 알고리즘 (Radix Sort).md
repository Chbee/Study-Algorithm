최초 작성 날짜 : 2025/04/02  
업데이트 날짜 : 2025/04/02  
공부 범위 : 05장      

---

## 기수 정렬
버킷 정렬(Bucket Sort)은 입력된 숫자 범위에 따라 많은 공간이 필요할 수 있다는 단점이 있다.  
이를 해결하기 위해 숫자의 각 자릿수를 기준으로 나누어 정렬하는 **Radix Sort**을 사용한다.

### 시간 복잡도
O(k × n)  
: 각 자릿수마다 전체 데이터를 한 번씩 순회하기 때문

#### 항상 보장되지는 않는 시간복잡도
기수 정렬은
1. 자릿수를 기준으로 나누고 
2. 버킷에 넣은 다음
3. 다시 이어 붙인다.

다시 이어붙이는 작업을 할 때, 값들이 **위치를 유지**해야만 한다.  
동일한 위치를 유지하는 것을 **안정성(stability)**이라고 한다.

따라서, 사용하는 서브 정렬 알고리즘에 따라 복잡도가 달라지겠지만
보통은 `O(k × n)`를 보장한다고 봐도 될듯하다.  

### Swift로 구현한 기수 정렬 (Radix Sort)
```swift
// 기수 정렬
func radixSort(_ arr: [Int]) {
    let radix = 10 // 0 ~ 9 까지의 자리
    var compareDone = false
    var digit: Int = 1 // 시작 자릿수

    var resultArray = arr // 복사본 생성

    while !compareDone {
        compareDone = true

        // 자릿수 만큼 버킷 생성
        var buckets = Array.init(repeating: [Int](), count: radix)
        
        resultArray.forEach { number in
            let index = number / digit // 자릿수 구하기
            // 비교하는 자릿수(digit)값을 분류
            buckets[index % radix].append(number)

            if compareDone && index > 0 {
                compareDone = false
            }
        }

        resultArray = buckets.flatMap { $0 } // 한개의 배열로 합침

        // 자릿수 변경
        digit *= radix        
    }
}
```
```swift
let radixData = [326, 12, 127, 467, 110, 58, 3, 999, 26]
```
이 데이터를 정렬해보자.

#### 1의 자리 비교
먼저 `digit`값이 `1`이므로 1의 자리부터 비교를 시작한다.
```swift
// 각 데이터별 index
// 326, 12, 127, 467, 110, 58, 3, 999, 26

// index값을 버킷의 크기로 나누면
// 6, 2, 7, 7, 0, 8, 3, 9, 6
// 이 된다.
// 따라서 버킷에는..
[[110], [], [12], [3], [], [], [326, 26], [127, 467], [58], [999]]
// 그러므로 resultArray에는 하나의 배열로 합쳐
// [110, 12, 3, 326, 26, 127, 467, 58, 999]
// 가 되고 다음 자리인 10의 자리로 비교한다.
```

#### 10의 자리 비교
```swift
// 1의자리 비교에서 새로운 배열을 구했다.
var resultArray = [110, 12, 3, 326, 26, 127, 467, 58, 999]
```
여전히 `compareDone`은 false이므로 비교가 다시 시작된다.
```swift
// 자릿수 값 : 1, 1, 0, 2, 2, 2, 6, 5, 9
// buckets
// [[3], [110, 12], [326, 26, 127], [], [], [58], [467], [], [], [999]]
// resultArray
// [3, 110, 12, 326, 26, 127, 58, 467, 999]
```
모든 자릿수(index)값이 0보다 크기 때문에 100의 자리수에서의 비교를 실행한다.

#### 100의 자리 비교
```swift
var resultArray = [3, 110, 12, 326, 26, 127, 58, 467, 999]
// 자릿수 값: 0, 1, 0, 3, 0, 1, 0, 4, 9
// buckets
// [[3, 12, 26, 58], [110, 127], [], [326], [467], [], [], [], [], [999]]
// resultArray
// [3, 12, 26, 58, 110, 127, 326, 467, 999]
```
여전히 0을 초과하는 index가 있기 때문에 1000의 자리를 비교한다.

#### 1000의 자리 비교
```swift
var resultArray = [3, 12, 26, 58, 110, 127, 326, 467, 999]
// 자릿수 값 : 0, 0, 0, 0, 0, 0, 0, 0, 0
```
마지막에 모든 숫자의 자릿수가 다 소진되어서 더 이상 비교할 자리가 없기 때문에 `compareDone`은 `true`가 되고, 반복문도 종료된다.
이때 결과값이 도출된다.
```swift
결과값 : [3, 12, 26, 58, 110, 127, 326, 467, 999]
```
