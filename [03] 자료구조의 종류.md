최초 작성 날짜 : 2025/03/26  
업데이트 날짜 : 2025/03/26  
공부 범위 : 04장  

---

자료구조는 데이터를 어떻게 효율적으로 저장하고, 꺼내고, 다룰지를 정하는 구조.  
각각의 자료구조는 사용 목적과 상황에 따라 장점이 다르기 때문에, 자료구조를 잘 이해하면 더 좋은 코드와 성능을 만들수 있다.

### 1. 배열
프로그램 언어중에서 가장 많이 사용되는 자료구조.
'동일한 형태'의 자료를 연속해서 저장한다.

#### 동일한 형태란
하나의 배열은 하나의 타입만 가질수 있다.
```swift
var strArr: [String] = []
strArr.append("첫번째")
strArr.append(2) // Error
```
기본 자료형은 물론이고 구조체, 클래스와 같은 사용자 정의 자료형을 의미한다.
```swift
// 사용자가 정의한 `SomeType(enum)`
enum SomeType {
case some
case other
case another
}

var someArr = [SomeType]()
someArr.append(.some)
```

### 2. 스택
LIFO<small> (Last In First Out)</small>
입력되는 순서대로 쌓이고 마지막에 들어온 것부터 사용한다.

**PUSH** : 스택 데이터 삽입  
**POP** : 스택 데이터 삭제

예를 들면 책을 쌓는것과 같다.  
책은 하나씩 위로 쌓고(PUSH), 가장 위의 책을 꺼낸다(POP)

#### 용도
대표적으로 프로그램을 수행하거나 수식의 연산을 수행하는 과정에서 사용한다.

`K = ((A*B)+(C/D))`
이 수식을 후위 표기법으로 변환해보자.
`이미지 작업 진행중`

#### 파이썬으로 구현한 스택
```python
def Stack() :
	stack = []

	stack.append(1) # [1]
	stack.append(2) # [1, 2]
	stack.append(3) # [1, 2, 3]
	stack.append(4) # [1, 2, 3, 4]
	stack.append(5) # [1, 2, 3, 4, 5]

	while stack :
		print("POP Value is ", stack.pop())
		 # POP Value is 5
		 # POP Value is 4
		 # POP Value is 3
		 # POP Value is 2
		 # POP Value is 1
Stack()
```
아주 간단하게 구현이 된다.

#### Swift로 구현한 스택
```swift
func stackExample() {
    var stack: [Int] = []

    stack.append(1)
    stack.append(2)
    stack.append(3)
    stack.append(4)
    stack.append(5)

    while !stack.isEmpty {
        let popValue = stack.removeLast()
        print("POP Value is", popValue)
    }
}

stackExample()
// 결과:
// POP Value is 5
// POP Value is 4
// POP Value is 3
// POP Value is 2
// POP Value is 1
```

### 3. 큐
FIFO<small> (First In First Out)</small>  
데이터가 입력되는 순서대로 쌓고, 먼저 들어온 것부터 사용하는 자료구조.

배열을 이용하여 순환 큐를 구현할 수 있고,  
연결 리스트를 이용하여 링크드 큐를 구현할 수 있다.

#### 파이썬으로 구현한 Queue
```python
def Queue() :

	queue = []
	queue.append(1) # [1]
	queue.append(2) # [1, 2]
	queue.append(3) # [1, 2, 3]
	queue.append(4) # [1, 2, 3, 4]
	queue.append(5) # [1, 2, 3, 4, 5]
	print(queue)

	while queue :
		print("Get Value : ", queue.pop(0))
		# Get Value : 1
		# Get Value : 2
		# Get Value : 3
		# Get Value : 4
		# Get Value : 5

Queue()
```

#### Swift로 구현한 Queue
```swift
func queueExample() {
    var queue: [Int] = []

    queue.append(1)
    queue.append(2)
    queue.append(3)
    queue.append(4)
    queue.append(5)
    print(queue)

    while !queue.isEmpty {
        let value = queue.removeFirst()
        print("Get Value :", value)
    }
}

queueExample()
// 결과:
// [1, 2, 3, 4, 5]
// Get Value : 1
// Get Value : 2
// Get Value : 3
// Get Value : 4
// Get Value : 5
```

### 4. 데크
Deque, **D**ouble-**E**nded **Que**ue의 줄임말  
리스트의 양쪽 끝에서 삽입과 삭제가 모두 이루어지는 자료구조.

스택과 큐를 혼합한 구조로 하나의 배열을 선언한 후, 2개의 포인터로 양쪽 끝을 가리키고 이것을 이용하여 양쪽에서 삽입, 삭제 연산을 수행함.

```
[Deque]
입력 ← [1, 2, 3, 4, 5] → 입력
       ↑           ↑
      삭제         삭제
```

#### 용도
여러 용도 중에서 이해한 것은 캐러셀 UI 즉, 무한 Scroll UI..  
최근 사용 목록 (MRU, LRU 캐시)이나 슬라이딩 윈도우 알고리즘에서도 사용한다고 한다.

#### 종류
입력 제한 데크: 스크롤, 삽입이 한쪽에서만 일어남  
출력 제한 데크: 셀프, 삭제가 한쪽에서만 일어남

#### Swift에서 구현한 데크
```swift
struct Deque<T> {
    private var array: [T] = []

    mutating func appendLeft(_ element: T) {
        array.insert(element, at: 0)
    }

    mutating func appendRight(_ element: T) {
        array.append(element)
    }

    mutating func popLeft() -> T? {
        return array.isEmpty ? nil : array.removeFirst()
    }

    mutating func popRight() -> T? {
        return array.isEmpty ? nil : array.removeLast()
    }

    func isEmpty() -> Bool {
        return array.isEmpty
    }

    func peek() -> T? {
        return array.first
    }
}
```

### 5. 트리
임의의 노드에서 다른 노드로의 경로가 하나 밖에 없는 자료구조

```
        10          ← 루트 노드
       /  \
      5    15       ← 자식 노드
     / \   / \
    2  7 12  20     ← 손자 노드들
```

노드 중에서 단 하나의 루트 노드(Root Node)가 있고, 루트 노드에서 하위 노드(Sub Node)들이 연결된 비선형 계층 구조.

#### 용도
**데이터 간의 계층적 관계**를 표현할 때 사용
- 예를 들어:
    - 폴더/파일 구조
    - 회사 조직도
    - 게임의 선택지 분기

위 예시 모두 위에서 아래로 한 단계씩 내려가며 관계를 표현함

#### 이진트리
트리 구조 중에서도 다음 데이터를 가리키는 것이 2개인 단방향 트리 구조를 `이진트리`라고 함  
루트 값과 비교했을때 작으면 왼쪽 노드에, 크면 오른쪽 노드에 값을 가지도록 구성한다.

#### 이진트리의 종류
1. Full Binary Tree : 포화 이진 트리, 레벨의 노드가 꽉 차 있는 트리.
2. Complete Binary Tree : 완전 이진 트리, 마지막 레벨 전까지 노드가 꽉 차있고, 마지막 레벨의 왼쪽에서 오른쪽으로 노드가 채워져 있는 트리
3. Skewed Binary Tree : 편향 이진 트리, 왼쪽 혹은 오른쪽 서브 트리만을 가지는 트리

#### Python로 구현한 이진 트리
```Python
class Node(object): 	# 노드의 형식을 정의하는 부분 
	def __init__(self, data): 
		self.data = data 
		self.left = self.right = None

class BinarySearchTree(object) : 
	def __init__(self): 
		self.root = None 

	def insert(self, data): # 이진 트리에 데이터를 넣는 부분 
		self.root = self._insert_value(self.root, data) 
		return self.root is not None 
	
	def _insert_value(self, node, data):
	    if node is None:
	        node = Node(data)
	    else:
	        if data <= node.data:
	            node.left = self._insert_value(node.left, data)
	        else:
	            node.right = self._insert_value(node.right, data)
	    return node

	def find(self, key): # 이진 트리에서 데이터를 찾는 부분 
		return self._find_value(self.root, key) 

	def _find_value(self, root, key): 
		if root is None or root.data ==key: 
			return root is not None 
		elif key <root.data: 
			return self._find_value(root.left, key) 
		else : 
			return self._find_value(root.right, key) 
	def delete(self, key) : # 이진 트리에서 데이터를 지우는 부분 
		self.root, deleted = self._delete_value(self.root, key) 
		return deleted 
	def _delete_value(self, node, key) :
		if node is None : 
			return node, False 

		deleted=False 
		if key == node.data : 
			deleted = True 
			if node.left and node.right : 
				parent, child = node, node.right 
				while child.left is not None : 
					parent, child = child, child.left 
				child.left = node.left 
				if parent != node: 
					parent.left=child.right 
					child.right = node.right 
				node = child 
			elif node.left or node.right : 
				node = node.left or node.right 
			else: 
				node = None 
		elif key < node.data: 
			node.left, deleted = self._delete_value(node.left, key) 
		else : 
			node.right, deleted = self._delete_value(node.right, key) 
		return node, deleted 
	
	def DFTravel(self): # 데이터를 출력하는 부분 
		def _DFTravel(root): 
			if root is None: 
				pass 
			else : 
				print(root.data, end=' ') 
				_DFTravel(root.left) 
				_DFTravel(root.right) 
				_DFTravel(self.root) 
		def LFTravel(self): # 데이터를 출력하는 부분 
			def _LFTravel(root) : 
				if root is None : 
					pass
				else: 
					_LFTravel(root.left) 
					print(root.data, end=' ') 
					_LFTravel(root.right) 
			_LFTravel(self.root) 

	def LRTravel(self): # 데이터를 출력하는 부분 
		def _LRTravel(root) : 
			if root is None: 
				pass 
			else : 
				_LRTravel(root.left) 
				_LRTravel(root.right) 
				print(root.data, end=' ') 
		_LRTravel(self.root) 

	def layerTravel(self): # 데이터를 출력하는 부분 
		def _layerTravel(root) : 

			queue = [root] 
			while queue : 
				root = queue.pop(0) 
				if root is not None : 
					print(root.data, end=' ') 
					if root.left : 
						queue.append(root.left) 
					if root.right: 
						queue.append(root.right) 
		_layerTravel(self.root)
```



#### Swift로 구현한 이진트리 (BST)
```swift
class TreeNode {
    var value: Int
    var left: TreeNode?
    var right: TreeNode?

    init(_ value: Int) {
        self.value = value
    }
}

class BinarySearchTree {
    var root: TreeNode?

    func insert(_ value: Int) {
        root = insertHelper(root, value)
    }

    private func insertHelper(_ node: TreeNode?, _ value: Int) -> TreeNode {
        guard let node = node else { return TreeNode(value) }
        if value < node.value {
            node.left = insertHelper(node.left, value)
        } else {
            node.right = insertHelper(node.right, value)
        }
        return node
    }

    func inOrderTraversal(_ node: TreeNode?) {
        guard let node = node else { return }
        inOrderTraversal(node.left)
        print(node.value, terminator: " ")
        inOrderTraversal(node.right)
    }
}
```



### 6. 힙
이진 트리의 일종으로 여러 개의 값 중에서 가장 큰 값이나 가장 작은 값을 빠르게 찾을 수 있도록 구성된 자료구조.

- 최소 힙(Min Heap) : 부모 노드의 값이 항상 하위 노드의 값보다 **작은** 경우  
- 최대 힙(Max Heap) : 부모 노드의 값이 항상 하위 노드의 값보다 **큰** 경우

```
    [Min Heap 예시]
        1
      /   \
     3     5
    / \   /
   7   8  9
```

```
	[Max Heap 예시]
	    100
	   /   \
	 50     80
	/  \   /
 20  30 70
```

#### 용도
주로 실시간 랭킹 시스템이나, 최단 거리 구하는 로직과 같은곳에서 사용된다.

#### Swift로 구현한 Heap (Min Heap)
```swift
struct MinHeap {
    private var heap: [Int] = []

    mutating func insert(_ value: Int) {
        heap.append(value)
        siftUp(heap.count - 1)
    }

    mutating func removeMin() -> Int? {
        guard !heap.isEmpty else { return nil }
        if heap.count == 1 {
            return heap.removeFirst()
        }

        let min = heap[0]
        heap[0] = heap.removeLast()
        siftDown(0)
        return min
    }

    private mutating func siftUp(_ index: Int) {
        var child = index
        var parent = (child - 1) / 2

        while child > 0 && heap[child] < heap[parent] {
            heap.swapAt(child, parent)
            child = parent
            parent = (child - 1) / 2
        }
    }

    private mutating func siftDown(_ index: Int) {
        var parent = index
        while true {
            let left = 2 * parent + 1
            let right = 2 * parent + 2
            var minIndex = parent

            if left < heap.count && heap[left] < heap[minIndex] {
                minIndex = left
            }
            if right < heap.count && heap[right] < heap[minIndex] {
                minIndex = right
            }
            if minIndex == parent { break }
            heap.swapAt(parent, minIndex)
            parent = minIndex
        }
    }
}
```


### 7. 그래프

그래프는 **서로 연결된 것들(사람, 장소, 길 등)을 표현하는 구조**

- **노드(Node)**: 각각의 점(사람, 도시, 컴퓨터 등)  
- **간선(Edge)**: 두 점 사이의 연결선 (도로, 관계, 케이블 등)

예를 들어:
- 지도 앱에서는 **도시(노드)**와 **도로(간선)**를 그래프로 표현  
- 친구 추천 기능도, **사람(노드)**들 사이의 관계를 그래프로 표현한 것.


##### 노드? 간선?

- **노드(Node)**: 그래프의 점. 사람, 도시, 컴퓨터 등 어떤 대상이든 될 수 있다.  
```
[노드 예시]
   A   B   C   D
```

- **간선(Edge)**: 노드끼리 연결된 선.  
```
[간선 예시]
   A —— B —— C
        |
        D
```

- **사이클(Cycle)**: 연결된 선들을 따라 출발한 곳으로 다시 돌아올 수 있는 구조  
```
[사이클 예시]
   A —— B
   |    |
   D —— C
```

- **탐색(Search)**: 그래프 안의 모든 노드를 돌아다니며 살펴보는 과정  
  

##### 그래프를 탐색하는 방법

- 깊이 우선 탐색: DFS; Depth First Search
	- 시작 정점에서 한 방향으로 갈 수 있는 가장 먼 경로까지 탐색 시도
	- 더이상 갈곳이 없으면, 가장 마지막에 만났던 부모 노드로 돌아와 다른 방향 탐색 시도
	- **쉽게 말하자면,** 한 방향으로 쭉쭉 가다가, 더 이상 갈 곳이 없으면 뒤로 돌아가서 다른 길을 찾는 방식
- 너비 우선 탐색: BFS; Breadth First Search
	- 시작 정점에서 인접한 모든 정점들을 우선 탐색
	- 더 이상 방문하지 않은 정점이 없을 때까지 방문했던 정점들을 시작점으로 하여 모든 정점들을 차례로 재탐색
	- **쉽게 말하자면,** 가까운 곳부터 하나씩 둘러보고, 그 다음 단계로 넘어가는 방식

##### 신장 트리
그래프 안의 **모든 노드들을 연결하는 트리**
- **사이클(빙글빙글 도는 길)**이 없어야 하고,
- **모든 노드가 한 번씩 연결**되어 있어야 한다.

##### 최소 비용 신장 트리란?
모든 노드를 연결하되, **가장 적은 비용(가중치)**으로 연결한 구조
- **Prim 알고리즘**: 지금 연결된 것 중에서 **가장 비용이 적은 노드**를 하나씩 골라서 확장
- **Kruskal 알고리즘**: 전체에서 **가장 작은 간선부터 하나씩** 골라서 연결 (사이클은 제외)

#### Swift에서 구현하는 그래프
```swift
class Graph {
    var adjacencyList: [String: [String]] = [:]

    func addEdge(_ from: String, _ to: String) {
        adjacencyList[from, default: []].append(to)
        adjacencyList[to, default: []].append(from) // 무방향 그래프
    }

	// Depth First Search, 깊이 우선 탐색 방식
    func dfs(_ start: String) {
        var visited: Set<String> = []
        dfsHelper(start, &visited)
    }

    private func dfsHelper(_ node: String, _ visited: inout Set<String>) {
        if visited.contains(node) { return }
        visited.insert(node)
        print(node, terminator: " ")

        for neighbor in adjacencyList[node] ?? [] {
            dfsHelper(neighbor, &visited)
        }
    }

	// Breadth First Search, 너비 우선 탐색 방식
    func bfs(_ start: String) {
        var visited: Set<String> = [start]
        var queue: [String] = [start]

        while !queue.isEmpty {
            let node = queue.removeFirst()
            print(node, terminator: " ")

            for neighbor in adjacencyList[node] ?? [] {
                if !visited.contains(neighbor) {
                    visited.insert(neighbor)
                    queue.append(neighbor)
                }
            }
        }
    }
}
```
