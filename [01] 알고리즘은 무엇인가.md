작성 날짜 : 2025/03/19  
공부 범위 : 01장 ~ 03장

# 알고리즘은 무엇인가  

## 부울 대수와 컴퓨터 논리 회로
부울대수란,  
조지 부울(George Boole)이 한 말로, 논리적 표현을 수학적으로 표기할 때 참(True), 거짓(False) 그리고 숫자(0과1)라는 기본요소로 조립될수 있다라는 개념

부울대수를 기반으로 탄생한 컴퓨터 논리 회로, 부울 대수를 전기에 대입하여 표현함.  
- AND : A와 B 모두 전기가 들어오면 1 (곱셈)
- OR : A와 B중 하나라도 전기가 들어오면 1 (덧셈)
- NOT : 입력이 1이면 0으로, 0이면 1로 바꿈

예를들어 `(1A)(A+B)`라는 식을 논리 회로로 표현하면
```
1. (1A)는 1과 A가 AND로 연결됨
2. (1A)와 (A+B)는 AND로 연결됨
3. (A+B)는 OR로 연결됨
```
3가지로 표현할 수 있음

## 알고리즘?
주어진 문제를 해결하기 위한 절차
1. 정해진 입력에 정해진 출력이 나와야하고
2. 무한히 반복되어선 안된다.

#### 알고리즘의 종류
- Sort : 데이터를 오름차순이나 내림차순으로 배치하는 방법
- Search : 데이터 집합에서 특정 값을 찾아내는 방법
- SPM(String Pattern Matching) : 주어진 텍스트에서 특정 패턴을 찾아내는 방법
- Calculation : 수학이나 공학의 문제 해결을 위한 방법

#### SPARKS
Structured Programming : A Reasonable Komplete Set의 준말. 알고리즘을 기술하는 언어중 하나
선언문, 지정문, 조건문, 반복문, Procedure문, 프로시져 사이 사료 전달 방법, 입출력문, 주석문 총 8가지로 구성됨
- 선언문
``` swfit
// 변수나 상수 등을 정의하여 사용할 수 있도록 선언
var a: Int = 10
let pi: Double = 3.141592
```
- 지정문
``` swift
// 변수에 값을 할당
a = 15
```
- 조건문
``` swift
// 조건에 따라 프로그램 흐름을 제어
// if-else
if n > 0 { ... }
else { ... }

// guard-else
guard n > 0 else { ... }
```
- 반복문
``` swift
// 반복적인 작업을 수행
// While
func printNumbers(limit: Int) {
    var i = 1
    while i <= limit {  // limit 값까지 반복
        print(i)
        i += 1  // i 값 증가
    }
}
// For
for i in 0...100 { ... }
```
- Procedure문
``` swift
// 특정 작업을 수행하는 프로시저나 함수를 정의
// 코드가 모듈화되어 재사용이 용이함.
func add(a: Int, b: Int) -> Int {
    return a + b  // 두 수의 합을 반환하는 프로시저
}
```
- 프로시져 사이 사료 전달 방법
``` swift
// 프로시저나 함수 간에 데이터를 전달하는 방법
// 값 전달 (pass-by-value)이 기본이지만, 참조전달(pass-by-reference)도 가능
// 값 전달 (Pass by Value)
func increment(x: Int) -> Int {
    var x = x  // 매개변수 x 복사
    x += 1
    return x
}

// 참조 전달 (Pass by Reference)
func appendItem(list: inout [Int], item: Int) {
    list.append(item)
}

// 예시 사용
var x = 5
let y = increment(x: x)  // x는 변경되지 않음

var myList = [1, 2]
appendItem(list: &myList, item: 3)  // myList는 [1, 2, 3]으로 변경됨
```
- 입출력문
``` swift
// 사용자로부터 입력을 받거나 출력을 하는 문자.
print()
readLine()
```
- 주석문
`코드에 대한 설명을 추가하는 문장`

## 알고리즘 작성과 구조화
예. 1~100 까지의 숫자 중에서 홀수, 짝수 합을 구하여 출력하는 알고리즘 구현
``` swift
var oddTotal: Int = 0
var evenTotal: Int = 0
for num in 1...100 {
  if num % 2 == 0 { evenTotal += num }
  else { oddTotal += num }
}

// 이 알고리즘은 최소 단위 기준으로 작업을 분리하면 총 3가지가 된다.
// 홀수합, 짝수합을 구하는 부분
// 출력하는 부분
//
// 분리한것에 따라 함수를 구현하면
//
// 홀수의 합을 구하는 함수
func sumOfOdds(from start: Int, to end: Int) -> Int {
    var oddTotal = 0
    for num in start...end {
        if num % 2 != 0 {  // 홀수일 경우
            oddTotal += num
        }
    }
    return oddTotal
}

// 짝수의 합을 구하는 함수
func sumOfEvens(from start: Int, to end: Int) -> Int {
    var evenTotal = 0
    for num in start...end {
        if num % 2 == 0 {  // 짝수일 경우
            evenTotal += num
        }
    }
    return evenTotal
}

// 합을 출력하는 함수
func printSums() {
    let oddTotal = sumOfOdds(from: 1, to: 100)
    let evenTotal = sumOfEvens(from: 1, to: 100)
    
    print("홀수 합: \(oddTotal)")
    print("짝수 합: \(evenTotal)")
}

// 실행
printSums()

```
