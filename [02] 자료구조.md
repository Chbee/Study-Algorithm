최초 작성 날짜 : 2025/03/20  
업데이트 날짜 : 2025/03/24  
공부 범위 : 04장

# 컴퓨터의 자료형  

| 분류                  | 설명                                                         | 예시                                            |
|---------------------|------------------------------------------------------------|------------------------------------------------|
| **기본 자료형**         | 컴퓨터 언어나 시스템에서 제공하는 기본적인 자료형            | `Int`, `Float`, `Double`, `Char`, `Bool`, `Void` |
| **사용자 정의 자료형**    | 사용자가 직접 정의할 수 있는 복합 자료형                     | `Struct`, `Class`, `Enum`, `typealias`           |
| **자료구조**            | 데이터를 조직하고 처리하기 위한 구조적 방법                  | 배열, 스택, 큐, 트리, 그래프, 해시맵 등           |


## 자료 구조 구현 방법
구현 방법에는 크게 두가지가 있다.
1. 리스트
2. 연결리스트

### 1. 리스트  
일반적으로 배열(Array) 또는 동적 배열(Set)을 기반으로 한 자료구조
연속적인 메모리 공간에 배치됨
- 인덱스를 통해 빠르게 접근 가능
- 동적 배열은 크기가 가변적임
- 배열을 사용하는 리스트는 O(1)의 시간복잡도를 가짐
``` swift
var list = [1, 2, 3, 4, 5]
list.append(6)   // 끝에 추가 (O(1))
list.insert(0, at: 0)  // 첫 번째 위치에 삽입 (O(n))
list.remove(at: 2)  // 인덱스 2에 해당하는 값 제거 (O(n))
```

### 2. 연결리스트
데이터를 노드라는 구조체로 저장하며 각 노드는 데이터와 그 다음 노드를 가리키는 포인터를 가짐
서로 링크 되어있기 때문에 연속된 메모리 공간은 필요X
- 리스트의 앞이나 중간에서 삽입/삭제 시에는 O(1)의 시간복잡도를 가짐
``` swift
// 단방향 링크 연결 예시
// Node Class 생성
class Node {
    var value: Int  // 데이터와
    var next: Node? // 다음 노드 포인트 정보
    
    init(value: Int) {
        self.value = value
        self.next = nil
    }
}

// LinkedList에서 Node 정보 가지고 있음
class LinkedList {
    var head: Node?
    
    func append(value: Int) {
        let newNode = Node(value: value)
        if head == nil {
            head = newNode
        } else {
            var current = head
            while current?.next != nil {
                current = current?.next
            }
            current?.next = newNode
        }
    }
    
    func display() {
        var current = head
        while current != nil {
            print(current!.value)
            current = current?.next
        }
    }
}

// 실제 사용
var linkedList = LinkedList()
let node1 = linkedList.append(value: 1)
let node2 = linkedList.append(value: 2)
let node3 = linkedList.append(value: 3)
let node4 = linkedList.append(value: 4)
linkedList.display()
// Print -
// 1
// 2
// 3
// 4
```

### 왜 시간복잡도의 차이가 발생할까?
시간복잡도의 차이를 알기위해 배열과 연결리스트의 차이를 찾아본다

#### 1. 배열
정적인 자료구조. (static)
배열을 만들기 위해 미리 크기를 정해놓아야한다.
크기가 정해지면 그 크기만큼 연속된 메모리 주소를 할당 받는다.
때문에 크기 변경이 불가능하지만, 데이터가 **순차적**으로 저장되기 때문에 임의 접근(random access)가 가능해진다.

#### 2. 연결리스트
동적인 자료구조. (dynamic)
배열과 다르게 미리 크기를 정하지 않아도 만들 수 있다.
연결리스트는 위에서 코드로 보았듯이 Node가 있고
1. Node에 저장된 데이터 값과
2. 다음 데이터의 메모리 주소값

이 두가지가 있기 때문이다.
때문에 배열처럼 순차적인 메모리 할당이 되지 않아도 선형구조로 데이터를 저장할 수 있다.
다만, 연속적으로 메모리 주소를 할당받지 않기 때문에 임의 접근은 불가능하다.

|구조|특징|
|--|--|
|배열|정적구조. 메모리 크기 불변. 임의 접근 가능|
|연결리스트|동적구조. 메모리 크기 가변. 임의 접근 불가능|


#### 시간복잡도를 구해보자

1. 배열
```swift
func testArrayPerformance() {
    var array = [Int]()
}
```
10만개의 숫자를 배열에 추가해보자
```swift
let startTime1 = CFAbsoluteTimeGetCurrent()
for i in 0..<100000 {
    array.append(i)
}
let endTime1 = CFAbsoluteTimeGetCurrent()
print("데이터 추가에 걸린 시간 : \(endTime1 - startTime1) second")
```
정확한 측정을 위해 `CFAbsoluteTimeGetCurrent()`함수를 사용
내 맥북기준 약 **0.047초**가 소요되었다.
데이터 추가(Append)는 이론상 O(1)의 시간복잡도를 가진다. 데이터 삽입과 중간 접근을 통해 적당한 시간이 소요되었는지 확인해보자.

첫번째 인덱스에 데이터 삽입:
```swift
let startTime2 = CFAbsoluteTimeGetCurrent()
array.insert(-1, at: 0)
let endTime2 = CFAbsoluteTimeGetCurrent()
print("데이터 0번째 삽입에 걸린 시간 : \(endTime2 - startTime2) second")
```
이번에는 **0.0007초**가 소요되었다.
첫번째 인덱스에 추가되며 모든 인덱스가 1씩 뒤로 밀리기때문에 O(n)의 시간복잡도를 가지게되고 이는 Append보다 많은 시간이 소요될것으로 예상했으나 실제로는 훨씬 빠르다.

중간 인덱스 접근:
```swift
let startTime3 = CFAbsoluteTimeGetCurrent()
_ = array[50000]
let endTime3 = CFAbsoluteTimeGetCurrent()
print("데이터 중간 인덱스 접근에 걸린 시간 : \(endTime3 - startTime3) second")
```
O(1)의 시간복잡도를 가지는 중간 접근은 **0.0002초**가 소요되었다.

내부적으로 공간이 부족할경우 재할당하는 작업이 발생한다.
재할당이 발생되지 않도록 삽입 갯수를 대폭줄여 100개로 추가테스트 해보았다.
```swift
// Append : 0.0002 sec
// Insert at Heading : 0.0006 sec
// Middle Access : 0.0002 sec
```
배열의 재할당이 발생하지 않도록 주의하여 사용이 필요해보인다.

2. 연결리스트
Swift에서 기본으로 제공해주는 연결리스트 타입은 없으므로 위에서 구현한 `LinkedList`를 활용하여 테스트를 해보자.

먼저 배열과 동일하게 데이터 삽입 테스트를 먼저 해보자
```swift
func testLinkedListPerformance() {
    let list = LinkedList()

    let startTime1 = CFAbsoluteTimeGetCurrent()
    for i in 0..<10000 {
        list.append(i)  // 끝에 추가 (O(n))
    }
    let endTime1 = CFAbsoluteTimeGetCurrent()
    print("연결리스트 데이터 추가에 걸린 시간: \(endTime1 - startTime1) seconds")
}
```
맥북 성능이슈로 10만개가아닌 1만개 데이터만 삽입하도록 수정했다.
1만개 데이터 삽입에는 **5.13초**초가 소요되었다.
순차적으로 메모리가 할당되지 않기 때문에, 다음 노드를 저장하기 위해선 마지막 노드를 찾기위해 전체를 순회해야한다.
따라서 O(n)의 시간복잡도를 가지게된다.

첫번째 인덱스에 데이터 삽입:
```swift
// LinkedList에 insert함수 추가
func insertAtHead(_ value: Int) {
    let newNode = Node(value: value)
    newNode.next = head
    head = newNode
}
// testLinkedListPerformance함수에서 호출
let startTime2 = CFAbsoluteTimeGetCurrent()
list.insertAtHead(-1)
let endTime2 = CFAbsoluteTimeGetCurrent()
print("연결리스트 0번째 인덱스 데이터 추가에 걸린 시간: \(endTime2 - startTime2) seconds")
```
항상 맨앞에서 연결하므로 앞에 데이터를 삽입하는것은 O(1)의 시간복잡도를 가진다.
**1.072초**로 Append보다 확실히 빠른 성능을 보인다.

중간 인덱스 접근:
```swift
// LinkedList에 insert함수 추가
func get(at index: Int) -> Int? {
    var current = head
    var count = 0
    while current != nil {
        if count == index {
            return current?.value
        }
        count += 1
        current = current?.next
    }
    return nil
}
// testLinkedListPerformance함수에서 호출
let startTime3 = CFAbsoluteTimeGetCurrent()
_ = list.get(at: 50)
let endTime3 = CFAbsoluteTimeGetCurrent()
print("연결리스트 데이터 중간 인덱스 접근에 걸린 시간 : \(endTime3 - startTime3) second")
```
O(n)의 시간복잡도를 가지는 중간 접근은 **0.0004초**초가 소요되었다.

##### Append가 느려지는 이유는?  
배열이든 연결리스트든 Append는 같은 시간복잡도를 가지는 로직에비해 훨씬 오래된 시간이 소요된다.

배열은 내부적으로 공간이 부족할경우, 크기를 유동적으로 확장하며 새 메모리를 할당하고 복사하는 작업이 이루어진다.
연결리스트는 유동적으로 메모리가 할당되기 때문에 재할당의 문제는 없지만 포인터 연산 오버헤드가 발생할 수 있다.

이 때문에 전체 인덱스 검색을 1회만 실행하는 '중간 인덱스 접근'과 달리 실제 성능에서 과부하가 발생하기 때문에 더 오랜시간이 소요된다.


## 자료 구조 분류  
1. 단순구조
2. 선형구조
3. 비선형구조
4. 파일구조

### 1. 단순구조 (Simple Structures)
가장 기본적인 형태의 자료구조
정수, 실수, true or false, 문자 및 문자열과 같이 기본 자료형을 저장할 때 사용됨

### 2. 선형구조 (Linear Structures)
데이터가 순차적으로 나열되는 자료구조
일렬로 저장하는 방식으로 배열과 Linked List의 두 가지 사용
순차적 접근 방식이 요구됨

### 3. 비선형구조 (Non-linear Structures)
데이터가 선형적이지 않게 즉, 트리 형태와 같이 배치된 자료구조
트리와 그래프와 같은 자료구조 포함
다중 경로를 통해 서로 연결될 수 있고 특정 요소를 다른 요소와 연결하는 방법이 여러가지 일 수 있음
재귀적인 관계를 표현할 때 유용
* 트리 (Tree) : 계층적인 관계를 나타내는 자료구조
* 그래프 (Graph) : 노드들 간에 복잡한 관계를 나타내는 자료구조

### 4. 파일구조 (File Structures)
데이터를 기타 저장 매체에 저장하는 방법을 정의하는 자료구조
디스크에 데이터를 저장하는 방식을 최적화 함
데이터에 대한 접근, 저장, 수정, 삭제가 효율적으로 이루어질 수 있도록 설계
주로 영속적 데이터 저장에 사용
* 연속 파일 구조 (Sequential File): 데이터를 순차적으로 저장하는 방식
* 색인 파일 구조 (Indexed File): 색인 테이블을 사용해 데이터를 빠르게 검색할 수 있는 구조
* 혼합 파일 구조 (Hashed File): 해시 함수를 사용하여 데이터에 빠르게 접근하는 구조
