최초 작성 날짜 : 2025/04/09  
업데이트 날짜 : 2025/04/09  
공부 범위 : 05장      

---

## 교환 정렬
작은 것부터 큰 순서로 정렬할 때, 작은 키를 갖는 데이터를 찾아 앞 데이터와 교환하는 알고리즘
1. 정렬할 데이터를 대상으로 앞에서부터 인접한 두 개의 크기를 비교하여 작은 것이 앞으로 가도록 교환
2. 나머지 데이터에 대해서도 1을 실행하여 위치 교환
3. 끝까지 수행한 후에는 다시 처음부터 동일한 작업 반복

### 시간 복잡도
O(n²)  
: 모든 데이터에 대해 비교 작업을 수해하므로 n x n의 시간 복잡도가 발생한다.  
따라서, n²의 시간 복잡도가 발생한다고 볼 수 있다.

### Swift로 구현한 교환 정렬 (Exchange Sort)
```swift
func exchangeSort(_ arr: [Int]) {
    let arrCount = arr.count
    var copyArr = arr
    
    for i in 0..<arrCount-1 {
        for j in i+1..<arrCount {
            if copyArr[i] > copyArr[j] {
                copyArr.swapAt(i, j)
            }
        }
    }
    print("resultArray ", copyArr)
}
```

```swift
let rawData = [12, 326, 127, 467, 110, 58]
```
이 데이터를 정렬해보자.

### i = 0
`copyArray[0]`은 12이다.  
`j`는 1부터 시작하고, 마지막 인덱스까지 탐색을 진행한다.  
##### 교환 없음
12가 가장 작은 수 이므로, 위치 교환이 발생하지 않는다.  
`[12, 326, 127, 467, 110, 58]`

### i = 1
`copyArray[1]`은 326이다.  
`j`는 1부터 시작하고, 마지막 인덱스까지 탐색을 진행한다.  
##### 첫번째 교환 발생
`j`가 2일 때, `arr[2]`값은 127이므로 값을 교환한다.  
`[12, 127, 326, 467, 110, 58]`
##### 두번째 교환 발생
`j`가 4일때, `arr[4]`값은 110이므로 값을 교환한다.  
`[12, 110, 326, 467, 127, 58]`
##### 세번째 교환 발생
`j`가 5일때, `arr[5]`값은 58이므로 값을 교환한다.  
`[12, 58, 326, 467, 127, 110]`

### i = 2
`copyArray[2]`은 326이다.  
`j`는 3부터 시작하고, 마지막 인덱스까지 탐색을 진행한다.  
##### 첫번째 교환 발생
`j`가 4일 때, `arr[4]`값은 127이므로 값을 교환한다.  
`[12, 58, 127, 467, 326, 110]`
##### 두번째 교환 발생
`j`가 4일때, `arr[5]`값은 110이므로 값을 교환한다.  
`[12, 58, 110, 467, 326, 127]`

### i = 3
`copyArray[3]`은 467이다.  
`j`는 4부터 시작하고, 마지막 인덱스까지 탐색을 진행한다.  
##### 첫번째 교환 발생
`j`가 4일 때, `arr[4]`값은 326이므로 값을 교환한다.  
`[12, 58, 110, 326, 467, 127]`
##### 두번째 교환 발생
`j`가 4일때, `arr[5]`값은 127이므로 값을 교환한다.  
`[12, 58, 110, 127, 467, 326]`

### i = 4
`copyArray[4]`은 467이다.  
`j`는 4부터 시작하고, 마지막 인덱스까지 탐색을 진행한다.  
##### 첫번째 교환 발생
`j`가 4일 때, `arr[5]`값은 326이므로 값을 교환한다.  
`[12, 58, 110, 127, 326, 467]`

결과값 : `[12, 58, 110, 127, 326, 467]`

### 선택 정렬과는 무슨 차이가 있지?
일단 두 정렬 방식 모두 앞에서부터 하나씩 비교하면서 동일한 값을 앞으로 보낸다는 공통점이 있다.  
  
차이점은 아래처럼 표로 정리할 수 있는데  
| 구분    | 선택 정렬 (Selection Sort) | 교환 정렬 (Exchange Sort) |
| ----- | ---------------------- | --------------------- |
| 비교 방식 | 가장 작은 값을 찾고 한 번만 교환    | 작으면 그때그때 교환           |
| 교환 횟수 | 한 번                    | 여러 번 가능               |
| 성능 차이 | 교환이 적어서 성능 약간 더 좋음     | 교환이 많아서 느릴 수 있음       |


