최초 작성 날짜 : 2025/04/15  
업데이트 날짜 : 2025/04/15  
공부 범위 : 05장      

---

## 삽입 정렬
삽입 정렬은 마치 교환 정렬처럼 원소의 위치를 바꾸지만, 단순히 자리를 바꾸는 것과는 차이가 있다.  
**왼쪽에 이미 정렬된 구간**을 기준으로 **현재 값이 들어갈 위치를 하나하나 비교하면서** 값을 "밀어내고" 알맞은 자리에 삽입한다.


#### 잠깐, 비슷한게 있던거 같은데?
`교환 정렬`이 비슷하지만 다르다.  
인접한 원소끼리 비교하고 조건에 따라 **서로 자리를 바꾸는 방식**이다.  
이런 방식에서는 **항상 두 값을 'swap'**하는 것이 핵심이다.

### 시간 복잡도
O(n²)
: 삽입 정렬은 각 원소가 왼쪽에 있는 정렬된 구간에 삽입될 위치를 찾기 위해 최악의 경우 최대 n번 가까이 비교/이동하게 되므로, 전체적으로 O(n²)의 시간 복잡도를 가진다.

### Swift로 구현한 삽입 정렬 (Insert Sort)
```swift
func insertSort(_ arr: [Int]) {
    var copyArr = arr
    
    for j in 1..<copyArr.count {
        var key = copyArr[j]
        var index = j - 1
        
        while index >= 0 && copyArr[index] > key {
            copyArr[index + 1] = copyArr[index]
            index -= 1
        }

        copyArr[index + 1] = key
    }
    print("resultArray ", copyArr)
}
```

```swift
let rawData = [5, 2, 8, 6, 1, 9, 3, 7]
```
이 데이터를 정렬해보자.

### j = 1
`copyArr[1]`은 2이다.  
`index`는 0부터 시작하고, `copyArr[0]`의 값은 5이다.  
##### 첫번째 교환 발생 (index = 0)
5가 2보다 크므로 교환이 발생한다.  
`[2, 5, 8, 6, 1, 9, 3, 7]`  
그리고 `index`는 -1이 된다.  
이는 while문을 충족하지 않으므로 `copyArr[0]`에 아까 복사해뒀던 `2`를 삽입하고 다음 인덱스로 넘어간다.  

### j = 2
`copyArr[2]`은 8이다.  
`index`는 1부터 시작하고, `copyArr[1]`의 값은 5이다.  
##### 교환 없음
while문에서 `copyArr[index] > key`의 조건을 충족하지 못하므로 교환없이 복사본을 원래의 위치에 삽입만한다.  
`[2, 5, 8, 6, 1, 9, 3, 7]`

### j = 3
`copyArr[3]`은 6이다.  
`index`는 2부터 시작하고, `copyArr[2]`의 값은 8이다.  
##### 첫번째 교환 발생 (index = 2)
8이 6보다 크므로 교환이 발생한다.  
`[2, 5, 6, 8, 1, 9, 3, 7]`  
그리고 `index`는 1이된다.
##### 교환 없음 (index = 1)
`copyArr[1]`은 5가되고, 이는 6보다 작다. 따라서 while의 조건을 충족하지 못하므로 더이상의 교환은 발생하지 않는다.
`[2, 5, 6, 8, 1, 9, 3, 7]`  

### j = 4
`copyArr[4]`은 1이다.  
`index`는 3부터 시작하고, `copyArr[3]`의 값은 8이다.  
##### 첫번째 교환 발생 (index = 3)
8이 1보다 크므로 교환이 발생한다.  
`[2, 5, 6, 1, 8, 9, 3, 7]`  
그리고 `index`는 2가된다.
##### 두번째 교환 발생 (index = 2)
`copyArr[2]`의 값은 6이고, 이는 아까 복사해둔 1보다 크므로 교환이 발생한다.
`[2, 5, 1, 6, 8, 9, 3, 7]`  
그리고 `index`는 1가된다.
##### 세번째 교환 발생 (index = 1)
`copyArr[1]`의 값은 5이고, 이는 아까 복사해둔 1보다 크므로 교환이 발생한다.
`[2, 1, 5, 6, 8, 9, 3, 7]`  
그리고 `index`는 0이된다.
##### 마지막 교환 발생 (index = 0)
`copyArr[0]`의 값은 2이고, 이는 아까 복사해둔 1보다 크므로 교환이 발생한다.
`[1, 2, 5, 6, 8, 9, 3, 7]`  
그리고 `index`는 -1이된다.
##### 교환 없음 (index = -1)
while 조건문을 충족하지 않으므로 더이상의 교환이 발생하지 않는다.  
`[1, 2, 5, 6, 8, 9, 3, 7]`  

### j = 5
`copyArr[5]`은 9이다.  
`index`는 4부터 시작하고, `copyArr[4]`의 값은 8이다. 
##### 교환 없음
8은 9보다 작으므로 while 조건문을 충족하지 않는다.  
`[1, 2, 5, 6, 8, 9, 3, 7]`  

### j = 6
`copyArr[6]`은 3이다.  
`index`는 5부터 시작하고, `copyArr[5]`의 값은 9이다. 
##### 첫번째 교환 발생 (index = 5)
9가 3보다 크므로 교환이 발생한다.  
`[1, 2, 5, 6, 8, 3, 9, 7]`  
그리고 `index`는 4가된다.
##### 두번째 교환 발생 (index = 4)
`copyArr[4]`의 값은 8이고, 이는 아까 복사해둔 3보다 크므로 교환이 발생한다.
`[1, 2, 5, 6, 3, 8, 9, 7]`  
그리고 `index`는 3이된다.
##### 세번째 교환 발생 (index = 3)
`copyArr[3]`의 값은 6이고, 이는 아까 복사해둔 3보다 크므로 교환이 발생한다.
`[1, 2, 5, 3, 6, 8, 9, 7]`  
그리고 `index`는 2가된다.
##### 네번째 교환 발생 (index = 2)
`copyArr[2]`의 값은 5이고, 이는 아까 복사해둔 3보다 크므로 교환이 발생한다.
`[1, 2, 3, 5, 6, 8, 9, 7]`  
그리고 `index`는 1이된다.
##### 교환 없음 (index = 1)
`copyArr[1]`의 값은 2이고, 이는 아까 복사해둔 3보다 작으므로 교환이 발생하지 않는다.
`[1, 2, 3, 5, 6, 8, 9, 7]`  

### j = 7
`copyArr[7]`은 7이다.  
`index`는 6부터 시작하고, `copyArr[6]`의 값은 9이다. 
##### 첫번째 교환 발생 (index = 6)
9가 7보다 크므로 교환이 발생한다.  
`[1, 2, 3, 5, 6, 8, 7, 9]`  
그리고 `index`는 5가된다.
##### 두번째 교환 발생 (index = 5)
`copyArr[5]`의 값은 8이고, 이는 아까 복사해둔 7보다 크므로 교환이 발생한다.
`[1, 2, 3, 5, 6, 7, 8, 9]`  
그리고 `index`는 4가된다.
##### 교환 없음 (index = 4)
`copyArr[4]`의 값은 6이고, 이는 아까 복사해둔 7보다 작으므로 교환이 발생하지 않는다.
`[1, 2, 3, 5, 6, 7, 8, 9]`   

결과값 : `[1, 2, 3, 5, 6, 7, 8, 9]`
