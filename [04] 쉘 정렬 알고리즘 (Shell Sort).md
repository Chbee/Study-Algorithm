최초 작성 날짜 : 2025/04/15  
업데이트 날짜 : 2025/04/17  
공부 범위 : 05장      

---

## 쉘 정렬
삽입 정렬 알고리즘의 느린 속도를 보완하기 위해 만들어진 알고리즘.  
데이터의 그룹을 나누어 그룹 안에서 쉘 정렬을 수행한 후 마지막에 삽입 정렬을 수행한다.  

### 말로 써보는 쉘 정렬 알고리즘
1. 배열 길이의 절반을 `gap`으로 정함
2. `gap` 간격으로 떨어진 숫자들끼리 삽입 정렬함
3. 정렬이 끝나면 `gap`을 반으로 줄임
4. gap이 1이 될 때까지 반복함
5. 마지막에는 일반 삽입 정렬을 수행함

말로만 적으면 무슨 말인지 이해가 잘 안간다.  
코드로 구현을 해보고 한줄한줄 보며 이해해보자.  

### 시간 복잡도

쉘 정렬의 시간 복잡도는 사용하는 **gap 수열**에 따라 달라짐.

- 단순한 gap 수열 (예: n/2, n/4, ..., 1) → 평균 O(n^1.3 ~ n^1.5)  
- Hibbard, Sedgewick, Ciura 같은 수열 → O(n log² n)까지 향상 가능  
- gap을 잘못 선택하면 최악의 경우 O(n²)도 나올 수 있음  

➡️ gap 수열의 선택이 중요함!  

##### 간단하게 알아보자 Gap 수열
Shell Sort에서 정렬할 때 사용하는 "간격(gap)"의 변화 규칙  
처음에 얼마나 떨어진 요소들끼리 비교할 것인지, 그리고 그 간격을 어떻게 줄여갈 것인지를 결정하는 숫자들의 나열  

### Swift로 구현한 쉘 정렬 (Shell Sort)
```swift
func shellSort(_ arr: [Int]) {
    var copyArr = arr
    // 배열 길이의 절반부터 시작해서 점점 줄여가는 간격(gap)으로 정함
    var gap = copyArr.count / 2

    // gap이 0이 되면 정렬이 끝났다는 것
    while gap > 0 {
        for start in 0..<gap {
            var i = start + gap
            // 삽입 정렬 적용
            while i < copyArr.count {
                // 지금 정리하려는 숫자를 temp에 저장
                let temp = copyArr[i]

                // j는 temp가 들어갈 위치를 찾기 위한 인덱스
                var j = i

                // j보다 앞에 있는 값이 temp보다 크면 그 값을 한 칸 뒤로 밈
                // 그 자리를 비워서 temp가 들어갈 자리를 만듬
                while j >= gap && copyArr[j - gap] > temp {
                    copyArr[j] = copyArr[j - gap]  // 한 칸 뒤로 복사
                    j -= gap               // 비교 위치를 앞쪽으로 이동
                }

                // temp가 들어갈 자리에 도착했으면 그 자리에 삽입
                copyArr[j] = temp

                // 다음 간격 위치로 넘어가서 같은 줄 안에 있는 다음 숫자 정리
                i += gap
            }
        }

        // 한 줄 안의 숫자들을 다 정리했으면, 간격을 반으로 줄여서 더 촘촘하게 정리
        gap /= 2
    }
    print("resultArray ", copyArr)
}
```
```swift
let rawData = [9, 8, 7, 6, 5, 4, 3, 2]
```

## 🔹 gap = 4

| start | i | temp | 비교 대상 (arr[j - gap]) | 이동 여부 | 결과 배열 |
|-------|---|------|--------------------------|------------|---------------------------|
| 0     | 4 | 5    | arr[0] : 9               | ✅ 이동     | [5, 8, 7, 6, 9, 4, 3, 2]  |
| 1     | 5 | 4    | arr[1] : 8               | ✅ 이동     | [5, 4, 7, 6, 9, 8, 3, 2]  |
| 2     | 6 | 3    | arr[2] : 7               | ✅ 이동     | [5, 4, 3, 6, 9, 8, 7, 2]  |
| 3     | 7 | 2    | arr[3] : 6               | ✅ 이동     | [5, 4, 3, 2, 9, 8, 7, 6]  |

---

## 🔹 gap = 2

| start | i | temp | 비교 대상 (arr[j - gap]) | 이동 여부 | 결과 배열 |
|-------|---|------|--------------------------|------------|---------------------------|
| 0     | 2 | 3    | arr[0] : 5               | ✅ 이동     | [3, 4, 5, 2, 9, 8, 7, 6]  |
| 0     | 4 | 9    | arr[2] : 5               | ❌ 유지     | [3, 4, 5, 2, 9, 8, 7, 6]  |
| 0     | 6 | 7    | arr[4] : 9               | ✅ 이동     | [3, 4, 5, 2, 7, 8, 9, 6]  |
| 1     | 3 | 2    | arr[1] : 4               | ✅ 이동     | [3, 2, 5, 4, 7, 8, 9, 6]  |
| 1     | 5 | 8    | arr[3] : 4               | ❌ 유지     | [3, 2, 5, 4, 7, 8, 9, 6]  |
| 1     | 7 | 6    | arr[5] : 8               | ✅ 이동     | [3, 2, 5, 4, 7, 6, 9, 8]  |

---

## 🔹 gap = 1 (삽입 정렬 단계)

| i | temp | 비교 대상 (arr[j - 1]) | 이동 여부 | 결과 배열 |
|---|------|------------------------|------------|---------------------------|
| 1 | 2    | arr[0] : 3             | ✅ 이동     | [2, 3, 5, 4, 7, 6, 9, 8]  |
| 2 | 5    | arr[1] : 3             | ❌ 유지     | [2, 3, 5, 4, 7, 6, 9, 8]  |
| 3 | 4    | arr[2] : 5             | ✅ 이동     | [2, 3, 4, 5, 7, 6, 9, 8]  |
| 4 | 7    | arr[3] : 5             | ❌ 유지     | [2, 3, 4, 5, 7, 6, 9, 8]  |
| 5 | 6    | arr[4] : 7             | ✅ 이동     | [2, 3, 4, 5, 6, 7, 9, 8]  |
| 6 | 9    | arr[5] : 7             | ❌ 유지     | [2, 3, 4, 5, 6, 7, 9, 8]  |
| 7 | 8    | arr[6] : 9             | ✅ 이동     | [2, 3, 4, 5, 6, 7, 8, 9]  |


결과값 : `[2, 3, 4, 5, 6, 7, 8, 9]`

